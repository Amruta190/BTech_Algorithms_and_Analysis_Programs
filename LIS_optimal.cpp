#include<iostream>
using namespace std;
void LIS(int arr[],int n)
{
    
}
int main()
{
    cout<<"Enter array size : ";
    int n;
    cin>>n;
    cout<<"Enter array elements : ";
    int arr[n];
    for(int i=0;i<n;i++)
        cin>>arr[i];
    LIS(arr,n);
}